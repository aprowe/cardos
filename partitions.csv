# CardOS partition table for the 8 MB ESP32-S3FN8 on the Cardputer.
#
# This deliberately matches partitions_cardputer.csv from the sibling
# CardLaunch project, because the apps we want to boot were built against that
# table and a guest reads the partition table it finds -- ours -- not the one
# it was compiled with. Diverging here is what makes a third-party app fail at
# startup for no visible reason.
#
# The USB-flashed CardOS lives in `factory` and is never overwritten. Two OTA
# slots alternate for everything else: a CardOS update fetched over WiFi, or a
# selected guest app copied from the card. Either goes into whichever slot is
# not running, so neither can take the running CardOS down. Two slots rather
# than one because you cannot overwrite the partition you are running from,
# and the OS has to be able to update itself more than once.
#
# Two independent ways home, neither needing the guest's cooperation:
#
#   1. The guest (or a new CardOS) is marked pending-verify. A guest never
#      confirms itself, so the next reset rolls back to the last CardOS that
#      did. A new CardOS confirms itself once its shell is up; one that
#      crashes first is rolled back the same way.
#   2. Erasing otadata leaves nothing valid to read, and the bootloader falls
#      back to factory. This is tools/back-to-cardos.py, and it is the escape
#      hatch when everything else has gone wrong.
#
# `spiffs` is small because CardOS keeps everything on the card and only
# Arduino guests look for it; they need it to exist and mount, not to be big.
# It was 3.3 MB before the second OTA slot took the space.
#
# The first three entries sit at their conventional offsets on purpose. The
# Arduino platform flashes boot_app0.bin at a hardcoded 0xe000 and the app at
# 0x10000 regardless of what this file says; CardLaunch found that moving them
# produced a bootloader reset loop, because the app landed in a data partition
# and `factory` stayed empty. CardOS is ESP-IDF rather than Arduino and so is
# not itself subject to that, but the guests are, and they must land where
# their own build expects.
#
# The data partition is named `spiffs` because that is the label Arduino's
# LittleFS looks for. It holds a LittleFS image regardless of the name. Rename
# it and every Arduino guest that stores anything will fail to mount.
#
# phy_init sits near the end because the space at the front is spoken for.
#
# Sizes, revised 2026-09-12 after factory hit 93%. Measured rather than
# guessed: CardOS is 1.62 MB, the largest guest anyone boots is cardlet at
# 1.35 MB, and the two OTA slots were holding 2.94 MB each for that. Nearly
# 6 MB was reserved for images of at most 1.7 MB while the partition that was
# actually full got 1.75.
#
#   factory  2.75 MB   1.62 used, and it is the one that grows
#   ota_0    2.25 MB   a guest, or CardOS updating itself
#   ota_1    2.25 MB   the other half of the A/B pair
#   spiffs    700 KB   Arduino guests' LittleFS; it was 316 KB
#
# nvs stays at 0x9000: moving it would throw away WiFi credentials, hotkeys
# and the Google token for no gain. The OTA slots moving does invalidate a
# staged guest, which the next boot re-copies from the card.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xE000,   0x2000,
factory,    app,  factory,  0x10000,  0x2C0000,
ota_0,      app,  ota_0,    0x2D0000, 0x240000,
ota_1,      app,  ota_1,    0x510000, 0x240000,
phy_init,   data, phy,      0x750000, 0x1000,
spiffs,     data, spiffs,   0x751000, 0xAF000,
